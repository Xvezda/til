{
  "name": "hooks-test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "prebuild": "./hooks/prebuild",
    "build": "echo fake build",
    "postbuild": "./hooks/postbuild",
    "success": "echo pseudo success && true",
    "postsuccess": "echo 'will it echo?'",
    "fail": "echo pseudo fail && false",
    "postfail": "echo 'will it echo?'"
  }
}
